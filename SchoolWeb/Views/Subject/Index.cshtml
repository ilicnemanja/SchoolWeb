@model IEnumerable<Subject>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3 mt-4">
    <div class="row pt-4">
        <div class="col-6">
            <h1 class="text-muted">Subject List</h1>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-controller="Subject" asp-action="Create"><i class="bi bi-plus-circle"></i> &nbsp; Create New Subject</a>
        </div>
        
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-bordered table-striped mt-3" style="width: 100%">
            <thead>
                <tr class="table-dark">
                    <th>Subject Name</th>
                    <th>Action Update</th>
                    <th>Action Remove</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr class="table-light">
                        <td width="50%">@obj.Name</td>
                        <td width="25%">
                            <div class="w-100 btn-group" role="group">
                                <a class="btn btn-outline-success mx-2" asp-controller="Subject" asp-action="Edit" asp-route-id="@obj.ID"><i class="bi bi-pencil-square"></i> Edit</a>
                            </div>
                        </td>
                        <td width="25%">
                            <form method="POST" action="Delete" class="w-100 btn-group" role="group">
                                <button method type="submit" class="btn btn-outline-danger mx-2" onclick="deleteConfirmation(event)" asp-controller="Subject" asp-action="Delete" asp-route-id="@obj.ID"><i class="bi bi-trash-fill"></i> Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>